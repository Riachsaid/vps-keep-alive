name: Stay Alive VPS

on:
  schedule:
    - cron: '*/5 * * * *' # يراقب كل 5 دقائق
  workflow_dispatch: # تقدر تطلقه بيدك

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Cloudflare Worker
        run: |
          curl -I https://cloud.klasiinkov.workers.dev
